{
    "type": "object",
    "title": "Google Maps Reviews Scraper",
    "description": "Provide your Apify API Token to connect the component with the Apify platform.",
    "schemaVersion": 1,
    "required": [
        "#token"
    ],
    "properties": {
        "#token": {
            "type": "string",
            "title": "Apify API Token",
            "format": "password",
            "description": "Enter your Apify API Token to start the actor. See the component description how to obtain the token."
        }
    }
}
